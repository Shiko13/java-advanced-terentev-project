apiVersion: v1
kind: Service
metadata:
  name: {{ include "main-service.fullname" . }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "main-service.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http